var express = require('express');
var router = express.Router();
var Department = require('../db').Department;
var Users = require('../db').User;

module.exports = router;

// router.get('/')

// router.post('/')

// router.delete('/:usrId')

//router.put('/:usrId')
//makes customer into employee